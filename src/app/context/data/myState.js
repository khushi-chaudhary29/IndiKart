/* import React from 'react'
import MyContext from './myContext'


const myState = (props) => {

    const state ={
        name:'vivek',
        rollno :15
    }
 
    const product= {
      title:"hello",
      price:320,
      img:"./Gaming.png"
    }
  return (
    <MyContext.Provider value={{state , product}}>
        {props.children}
    </MyContext.Provider>
  )
}

export default myState */